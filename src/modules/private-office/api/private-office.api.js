class PrivateOfficeAPI {
    list (buildingId) {
        // GET /api/buildings/${buildingID}/private-offices or whatever
    }
    get(privateOfficeId) {
        // GET from wherever
    }

    create(privateOffice) {
        // post privateOffice
    }

    update(privateOffice) {
        // PUT privateOffice
    }

    delete(privateOfficeId) {
        // DELETE ID
    }
}