class MeetingRoomBookingAPI {
    list() {
        // GET /api/user/bookings/private-offices or something
    }

    get(bookingId) {
        // GET from wherever
    }

    create(booking) {
        // post privateOffice
    }

    update(booking) {
        // PUT privateOffice
    }

    delete(bookingId) {
        // DELETE ID
    }
}